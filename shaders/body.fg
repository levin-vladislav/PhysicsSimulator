#version 330 core
out vec4 FragColor;
in vec2 localPos;
uniform int shape;
uniform float radius;

void main()
{
	float alpha = 1.0;
	if (shape == 1)
	{
		float d = length(localPos);
		if (d > radius) alpha = 0.0;
	}

	FragColor = vec4(0.8, 0.8, 0.8, alpha);
	
	
}